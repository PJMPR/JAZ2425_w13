rootProject.name = 'users-data-updater'

// NOTE: This folder is meant to be included from the root multi-module build (`../settings.gradle`).
// Keeping this file minimal avoids IntelliJ/Gradle treating it as a separate composite build.
